name: Deploy Kubernetes Manifests
on:
  push:
    branches:
      - main  # Triggers when you push to main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run your deployment script/commands
        run: |
          echo "Deploying Kubernetes gateway and manifests..."
          # You can add your kubectl, docker, or custom bash script commands here
          # e.g., kubectl apply -f minikube/
